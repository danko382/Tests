\textbf{ѕравило произведени€:}
\smallskip

    ≈сли объект $a$ можно выбрать $m$ способами,
    а объект $b$, после выбора $a$, можно выбрать
    $n$ способами, то пару $(a, b)$ можно выбрать
    \textbf{\dots} способами.

    \begin{enumerate}
        \item $n + m$
        \item $n$
        \item $n * m$
        \item $m$
    \end{enumerate}
\bigskip

\textbf{ѕравило суммы:}
\smallskip
    
    ≈сли $A$ можно выбрать $n$ способами, а объект $B$
    $m$ способами, то объект $A$ или $B$ можно выбрать
    \textbf{\dots} способами. (¬ыбор $B$ никак не согласуетс€
    с выбором $A$.)

    \begin{enumerate}
        \item $n + m$
        \item $n$
        \item $n * m$
        \item $m$
    \end{enumerate}
\bigskip

\textbf{ќпределение (–азмещение с повторени€ми):}
\smallskip

    –азмещени€ми с повторени€ми из $n$ типов по $k$ элеметов
    ($k$ и $n$ \textbf{1) \dots}) называютс€ все
    такие последовательности $k$ элементов, принадлежащие
    $n$ типам, которые отличаютс€ друг от друга \textbf{2) \dots}
    элементов.
    
    \[
        \textbf{3) \dots}  
    \] 
    
    \begin{enumerate}
        \item{1) в произвольном соотношении; 2)составом;
        3) $\overline{A}^k_n = n^k$}
        \item{1) : $n < k$; 2)составом или последовательностью;
        3) $\overline{A}^k_n = n^k$}
        \item{1) в произвольном соотношении;
        2)последовательностью; 3) $A^k_n = n^k$}
        \item{1) в произвольном соотношении;
        2)составом или последовательностью;
        3)$\overline{A}^k_n = n^k$}
    \end{enumerate}
\bigskip

\textbf{ќпределение (–азмещени€ без повторений):}
\smallskip

    –азмещени€ми без повторений из $n$ различных типов
    по $k$ элементам называютс€ все такие последовательности
    из $k$ различных элементов такие, что они отличаютс€ 
    друг от друга \textbf{1) \dots} элементов. ѕричЄм
    \textbf{2) \dots}.

    \[
        \textbf{3) \dots}  
    \]

    \begin{enumerate}
        \item{1) составом;
        2) $k$ и $n$ в произвольном соотношении;
        3) $\overline{A}^k_n = n^k$}
        \item{1) составом или последовательностью;
        2)$k < n$;
        3) $A^k_n = \frac{n!}{(n - k)!}$}
        \item{1) последовательностью;
        2)$k < n$; 3) $\overline{A}^k_n = \frac{n!}{(n - k)!}$}
        \item{1) составом или последовательностью;
        2)$k$ и $n$ в произвольном соотношении;
        3)$\overline{A}^k_n = \frac{n!}{(n - k)!}$}
    \end{enumerate}